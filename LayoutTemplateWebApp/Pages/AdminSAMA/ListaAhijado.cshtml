@page
@model LayoutTemplateWebApp.Pages.AdminSAMA.ListaAhijadoModel
@{

    var userRole = @Model.role;
    Layout = userRole == "2569" ? "_AdminLayout" : "_ErrorLayout";

    if (userRole != "2569") Response.Redirect("/ErrorPage");
}
<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en">

<body>

    <div class="container">
        <h1 class="text-center">Ahijados Registrados:</h1>
        <div class="table-responsive px-4 py-3">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">Nombre</th>
                        <th scope="col">Apellidos</th>
                        <th scope="col">Carrera</th>
                        <th scope="col">Correo</th>
                        <th scope="col">Residencia</th>
                        <th scope="col">Gustos</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <th scope="row">Mario</th>
                        <td>Otto</td>
                        <td>Ingenieria en Computacion</td>
                        <td>mo@estudiantec.cr</td>
                        <td>San Jose, San Jose, Hospital</td>
                        <td>Musica, Helado</td>
                    </tr>
                    <tr>
                        <th scope="row">Ana</th>
                        <td>Maria Polo</td>
                        <td>Ingenieria en Derecho</td>
                        <td>amp@estudiantec.cr</td>
                        <td>San Jose, Moravia, La Isla</td>
                        <td>Musica, Leer</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

</body>

</html>
=======

@section Scripts
    {
    <script src="@Url.Content("~/js/Admin/GestionSolicitudes.js")"></script>
}

<section class="container px-5 py-3">
    <h3 style="margin-bottom: 2rem;">Lista de Personas</h3>
    <table class="table" aria-label="Tabla de nombres de personas">
        <pre>@Model.RawJsonData</pre>
        <thead>
            <tr>
                <th scope="col">Nombre</th>
                <th scope="col">Apellido</th>
                <th scope="col">Email</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.PersonList != null && Model.PersonList.Any())
            {
                foreach (var person in Model.PersonList)
                {
                    <tr>
                        <td>@person.PersonName</td>
                        <td>@person.FirstLastName</td>
                        <td>@person.Email</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="2">No persons found.</td>
                </tr>
            }
        </tbody>
    </table>
</section>
>>>>>>> danibrach
